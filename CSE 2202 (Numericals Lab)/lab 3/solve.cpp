#include<bits/stdc++.h>
using namespace std;

double f(double x){
    double ans = x*x*x*x - 3*x*x*x - 3*x*x + 11*x - 6;
    return ans;
}

int main(){

    double r = sqrt(15);
    double l = -r, e = 100;
    double a = l,b = a+,c;
    while()


    return 0;
}